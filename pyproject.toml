[project]
name = "lang-graph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langgraph>=1.1.9",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "ipykernel>=6.0.0",
]
